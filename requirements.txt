pelican==4.11.0
pagefind==1.3.0
pagefind_bin_extended==1.3.0
playwright>=1.44
pytest-playwright>=0.4

# For org-mode support
# Note: Requires Emacs to be installed on system
# Note: pelican-plugins is managed as a git submodule
pyyaml>=6.0
